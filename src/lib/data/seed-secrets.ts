import "server-only";
import type { ItemSecret } from "@/lib/types";

// Server-only. Imported ONLY by API routes that verify access first.
// Never import this into a client component or a public data path.

export const SEED_SECRETS: Record<string, ItemSecret> = {
  meridian: {
    itemId: "00000000-0000-4000-8000-000000000001",
    promptText: `ROLE & STACK
You are building Meridian, a single-screen SaaS landing page for a developer-facing product. Stack: Next.js (App Router), Tailwind CSS, TypeScript. No component library. Ship semantic HTML, accessible markup, and CSS that a senior front-end engineer would sign their name to. Assume the page ships behind a CDN and must be fast on a cold cache.

DESIGN THESIS
The single idea: restraint reads as credibility. Developers distrust landing pages that shout. Meridian commits to one luminous hero moment and lets everything else recede — quiet type, generous negative space, a single accent colour used sparingly enough that it still means something when it appears. No gradients-on-everything, no floating 3D blobs, no more than one animated element visible at a time. If a section could be described as "flashy," cut it.

TOKENS
Colour: background #0B0F14 (near-black, slightly blue), surface #11161D, surface-raised #161D26, border #1F2733, text-primary #E7ECF2, text-secondary #8B98A9, text-tertiary #5A6472. Accent: a cool electric indigo, #6C7CFF, with a hover/active state at #8C99FF and a muted 12%-opacity wash #6C7CFF1F for backgrounds. Success/code-diff green #3ECF8E used only inside the terminal proof block. Typography: headings in "Geist", -apple-system, sans-serif at weight 600 for display sizes and 500 for section headings; body in "Inter", sans-serif at weight 400; code in "Geist Mono", ui-monospace at weight 400. Type scale: hero 56px/1.05 desktop (36px/1.1 mobile), section heading 32px/1.15, body 17px/1.6, small 14px/1.5. Spacing scale is 4px-based: 4, 8, 12, 16, 24, 32, 48, 64, 96, 128. Section vertical rhythm is 128px desktop, 64px mobile. Radius: 8px for buttons and inputs, 14px for cards, 999px for pills/badges. Border width 1px at #1F2733. Blur: backdrop-blur of 12px on the sticky nav only.

SIGNATURE ELEMENT
The hero has exactly one animated element: a thin luminous horizon line, 2px tall, spanning the full hero width, sitting just below the headline. On load it draws itself left-to-right over 900ms using a stroke-dashoffset animation with cubic-bezier(0.16, 1, 0.3, 1) (ease-out-expo), then settles into a slow, near-imperceptible breathing glow — box-shadow blur radius oscillating between 8px and 20px of #6C7CFF at 40% opacity over a 4-second sine loop. This is the only continuous motion on the page. Nothing else pulses, floats, or auto-plays. The line is what the product name refers to: a meridian, a fixed reference point.

LAYOUT — SECTION BY SECTION
Nav: sticky, 72px tall, backdrop-blur-12px over background at 80% opacity, bottom border 1px #1F2733. Logo left, four text links centre-right, a bordered "Sign in" and a filled accent "Get started" button far right. Collapses to a logo + single filled CTA + hamburger under 768px.
Hero: max-width 1120px centred, 160px top padding desktop (96px mobile). Small pill badge above headline ("Now in public beta" style, 12px text, accent wash background). Headline: two lines max, 56px, text-primary, with one word in accent colour. Subhead: 18px text-secondary, max-width 560px, 24px top margin. Two CTAs side by side, 32px top margin: filled accent primary, ghost-bordered secondary. The horizon line sits 64px below the CTAs, full bleed within the 1120px container. Below it, a code-window screenshot mock (not a real screenshot — build it as HTML/CSS: macOS-style traffic-light dots, a monospace command, a faux syntax-highlighted body) in a card with 14px radius and a soft ambient shadow using the accent at 8% opacity.
Feature triptych: 96px top padding, three-column grid (1fr each, 32px gap, collapsing to single column under 768px). Each card: surface-raised background, 1px border, 32px padding, 14px radius, an icon rendered as a simple inline SVG stroke icon (24px, accent colour, no icon library), an 18px/600 title, 15px/1.6 text-secondary body of two sentences max.
Code-forward proof block: full-width surface section, 96px vertical padding. Left column (5/12): a claim headline and two sentences of supporting copy. Right column (7/12): a terminal-style code panel, dark #0B0F14, Geist Mono, with a simulated install-and-run sequence, green (#3ECF8E) for success lines, text-tertiary for comments.
Pricing teaser: not a full pricing table — a single centred card, 640px max-width, surface-raised, 14px radius, showing one plan summary with a price, three bullet checks (accent checkmark), and a single CTA linking to a full pricing page. Deliberately understated; it is a teaser, not a decision point.
Footer: 64px padding, border-top 1px #1F2733, logo + four link columns + copyright line in text-tertiary at 13px.

MOTION DOCTRINE
Page-load: hero content fades and rises 12px over 500ms at cubic-bezier(0.16, 1, 0.3, 1), staggered 80ms per element (badge, headline, subhead, CTAs, then the horizon-line draw). Scroll-triggered reveals for every section below the fold: opacity 0 to 1 and translateY 16px to 0 over 400ms ease-out, triggered once at 20% viewport intersection, never re-triggering on scroll-up. Hover states: buttons scale to 1.02 and shift accent brightness up one step over 150ms ease. No parallax, no scroll-jacking, no cursor-follow effects. prefers-reduced-motion: disable the horizon-line breathing glow and all staggered entrance animations entirely — content renders in its final state immediately, only the initial stroke-draw of the horizon line may remain but capped at 200ms.

PERFORMANCE BUDGET
First Load JS: under 90KB gzipped for the route (this page ships almost no client JS — the scroll reveals use an IntersectionObserver in a small client island, everything else is static). LCP target: under 1.8s on a throttled 4G profile, achieved by inlining critical CSS for the hero and using next/font with font-display: swap for Geist and Inter. CLS target: 0 — reserve exact heights for the code-window mock and all icons before hydration. Images: none required (the code-window is markup, not an image); if a real product screenshot is later swapped in, it must use next/image with explicit width/height and priority on the hero image only. No third-party scripts.

BUILD ORDER
Build one block at a time and stop after each for review.
1. Scaffold the Next.js App Router page, Tailwind config with the token values above as CSS variables, and font loading via next/font.
2. Build the nav bar, static and responsive, no scroll behaviour yet.
3. Build the hero section content and layout without the horizon-line animation — get spacing and type exactly right first.
4. Implement the horizon-line signature element: the SVG line, the draw-in animation, and the breathing glow, including the prefers-reduced-motion fallback.
5. Build the feature triptych with the inline SVG icons.
6. Build the code-forward proof block with the terminal panel.
7. Build the pricing teaser card and the footer.
8. Wire up the IntersectionObserver-driven scroll reveals as a single small client component, apply it to each section, and do a final pass checking the performance budget above.`,
    iterationNotes: `First pass over-animated the hero — three moving elements competed for attention, so the brief was tightened to exactly one continuous motion (the horizon line) with everything else limited to a single entrance stagger. Watch for the model wanting to add a hero background gradient or grid pattern; push back and keep the background flat #0B0F14, since the whole thesis depends on restraint. Also confirm it treats the pricing block as a teaser, not a full three-tier table — that's Halcyon's job, not Meridian's.`,
    assetBundlePath: "seed/meridian.zip",
  },
  halcyon: {
    itemId: "00000000-0000-4000-8000-000000000002",
    promptText: `ROLE & STACK
You are building Halcyon, a drop-in pricing section component. Stack: React (functional components, hooks), Tailwind CSS. This is not a page — it is a self-contained section meant to be imported into an existing dark landing page, so it must not assume any particular router, layout shell, or global CSS beyond Tailwind utilities. Export a single default component with no required props (sensible internal defaults) but allow tier data and copy to be overridden via props.

DESIGN THESIS
The single idea: answer the objection before it forms. Every pricing page fails the same way — the visitor hesitates on "is this worth it," "what happens if I need more," or "can I cancel." Halcyon resolves all three inline, next to the number that raised the question, rather than banishing them to a separate FAQ page. The highlighted plan is emphasised through scale and elevation, never through colour alone, so the section stays legible in isolation.

TOKENS
Colour: this section is designed to sit on a dark canvas, background transparent by default with an optional surface fallback #0E1116. Card surface #151A21, card surface (highlighted plan) #1A2130, border #232B36, border (highlighted) #3D4EE0 at 50% opacity. Accent: a saturated cobalt, #3D4EE0, hover state #5566F0, success/check colour a paired teal #2FBFA0. Text: primary #EDF1F7, secondary #9AA5B4, tertiary #5D6675. Typography: headings "Geist", sans-serif, weight 600; body "Inter", sans-serif, weight 400; price figures "Geist", weight 700, tabular-nums. Scale: section heading 34px/1.15, plan name 20px/600, price 44px/1 with unit label at 15px, body/feature-list 15px/1.6, small print 13px/1.5. Spacing: 4px base scale (4, 8, 12, 16, 24, 32, 48). Card padding 32px. Gap between three cards 24px desktop, stacking to 16px vertical gap under 768px. Radius: 16px on cards, 999px on the annual/monthly toggle pill, 6px on the small savings badge. Border width 1px, 2px on the highlighted card.

SIGNATURE ELEMENT
The annual/monthly toggle is the signature moment. It is a pill-shaped switch, 240px wide, 44px tall, with a sliding thumb that moves between "Monthly" and "Annual" over 280ms using cubic-bezier(0.4, 0, 0.2, 1). The moment it lands on "Annual," every price in all three cards cross-fades and counts down from its monthly-equivalent number to its annual-equivalent number over 420ms — implemented as a numeric tween (requestAnimationFrame interpolation, not a CSS transition, since it is animating a text value), eased with cubic-bezier(0.16, 1, 0.3, 1), rendered with tabular-nums so digits don't jitter horizontally. A small savings badge ("Save 18%" style, computed from the actual numbers passed in, never hardcoded) fades in beside the toggle 150ms after the switch completes.

LAYOUT — SECTION BY SECTION
Section wrapper: max-width 1120px centred, vertical padding 96px (56px mobile). Header block: centred, small eyebrow label in accent uppercase 12px tracked-out, section heading below it, one-sentence subhead in text-secondary, 16px gaps between the three. The monthly/annual toggle sits 32px below the subhead, centred.
Card row: three-column grid, 1fr 1.08fr 1fr so the centre (highlighted) card is visibly larger, 24px gap, collapsing to a single column under 768px with the highlighted card reordered to display first on mobile via CSS order.
Each card: plan name top-left, a one-line description in text-secondary below it, then the price block (big number, "/mo" unit, small annual-equivalent note underneath in tertiary when annual is active), then a full-width CTA button (ghost-bordered for the two outer plans, filled accent for the highlighted plan), then a divider, then a feature checklist — each line a teal check icon plus 15px text, with denser/more-inclusive plans literally listing more lines so the visual weight communicates value. The highlighted card additionally gets a small floating "Most popular" badge pinned to its top edge, translateY -50%, pill-shaped, filled accent background.
Inline objection-handling: under the CTA button on each card, one line of 13px tertiary text that pre-empts the likely hesitation for that tier specifically (e.g. the entry tier addresses "can I upgrade later," the top tier addresses "is there a discount for teams") — this copy must be tier-specific, not a repeated generic line.
FAQ: 64px below the card row, a two-column grid of question/answer pairs (single column mobile), five to six entries, each a plain text block (no accordion required, though an accordion variant is acceptable if it defaults open on the first item) closing objections that survived the pricing cards themselves — refund policy, what counts as a seat, what happens on downgrade, contract length, and one product-specific question.

MOTION DOCTRINE
Card entrance: on mount, the three cards fade and rise 12px over 400ms, staggered 80ms left to right, ease-out-expo (cubic-bezier(0.16, 1, 0.3, 1)) — only on first mount, never re-triggered by the toggle. Toggle interaction as described in SIGNATURE ELEMENT above is the only recurring animation. Feature checklist items have no individual animation — they are static once cards are in. Button hover: background brightens one step and the button lifts with a 2px translateY over 150ms. prefers-reduced-motion: disable the mount stagger (render immediately in final position) and replace the numeric count-up tween with an instant value swap; the toggle thumb still slides but over a flat 1ms (effectively instant) rather than 280ms.

PERFORMANCE BUDGET
This is a component, not a route, so the budget is expressed per-component: under 8KB gzipped of component JS excluding React itself, zero external dependencies beyond React and Tailwind's generated CSS, no images (all icons are inline SVG). The numeric tween runs on requestAnimationFrame and must be cancelled on unmount and on rapid re-toggling to avoid stacked animation frames. CLS: 0 — the toggle and price swap must not reflow card height, so reserve a fixed min-height on the price block sized to the longer of the two price string lengths. No layout thrash: batch the DOM reads/writes in the tween.

BUILD ORDER
Build one block at a time and stop after each for review.
1. Build the static three-card layout with hardcoded monthly prices, correct spacing, and the highlighted-card scale treatment — no toggle yet.
2. Build the FAQ grid below it.
3. Add the toggle pill component in isolation (visual only, sliding thumb, no price logic wired yet).
4. Wire the toggle to state and implement the instant (non-animated) price swap between monthly and annual values plus the savings badge.
5. Replace the instant swap with the requestAnimationFrame numeric tween, including the reduced-motion fallback.
6. Add the mount stagger animation for the card row and the tier-specific objection-handling lines under each CTA.
7. Final pass: prop typing so tier data, prices and FAQ entries are overridable, and a check that everything still reads correctly with only one tier of data (defensive against the "1.08fr centre column" assumption breaking with two or four tiers).`,
    iterationNotes: `The numeric count-up on the price toggle was originally a CSS transition on the text content, which doesn't animate — had to be explicit that it needs a requestAnimationFrame tween interpolating the underlying number. Watch for the model hardcoding the savings percentage instead of computing it from the passed-in monthly/annual figures, which breaks the moment someone changes a price. Also check the mobile reorder of the highlighted card actually uses CSS order rather than DOM reordering, so it stays accessible.`,
    assetBundlePath: "seed/halcyon.zip",
  },
  "obsidian-studio": {
    itemId: "00000000-0000-4000-8000-000000000003",
    promptText: `ROLE & STACK
You are building Obsidian Studio, a full multi-section site for a design/branding agency. Stack: Next.js (App Router), Tailwind CSS, Framer Motion, TypeScript. This is a complete site build (multiple sections composed on a single scrolling homepage, structured so individual sections could later be split into routes for case-study detail pages). Treat this like an editorial layout job, not a SaaS template — the client is a small studio that wants to look inevitable, not scrappy.

DESIGN THESIS
The single idea: the confidence of a monograph. This site should read like a printed art-book, not a web template — asymmetric grids, oversized display type, generous margins, and a near-absence of chrome (no rounded pill buttons, no card shadows, no gradient accents). Colour is almost entirely absent except for one warm accent used as punctuation. If a section looks like it could belong to any agency, it hasn't earned its place yet.

TOKENS
Colour: background #100F0D (warm near-black, not blue-black), surface #17150F, text-primary #F4F1E9 (warm off-white, not pure white), text-secondary #A39C88, text-tertiary #6B6558, border #2B271F. Accent: a single warm burnt-clay tone, #C1603A, used only for the one signature underline/highlight moment and link hovers — never as a background fill on large areas. Typography: display headings in "Fraunces", serif, weight 400-600 variable, used at large sizes for editorial voice; body and UI text in "Inter", sans-serif, weight 400/500; small caps labels in Inter weight 500 with 0.08em letter-spacing. Scale: hero display 96px/0.95 desktop (48px/1.0 mobile), section heading 48px/1.05, case-study title 32px/1.1, body 17px/1.65, small label 12px uppercase tracked. Spacing scale is looser than a typical SaaS site: 8px base doubling to 8, 16, 24, 32, 48, 64, 96, 144, 192. Section rhythm: 192px vertical padding desktop, 96px mobile — this generosity is load-bearing for the editorial feel. Radius: 0px everywhere except a single 2px on form inputs; sharp corners are part of the thesis. No blur, no box-shadow except a barely-there 1px hairline on the works-grid hover state.

SIGNATURE ELEMENT
Each entry in the selected-works grid reveals its title with an animated clay-coloured underline that draws itself left-to-right on hover (or on scroll-into-view on touch devices), 3px tall, using the accent #C1603A, over 320ms at cubic-bezier(0.65, 0, 0.35, 1). Simultaneously the project's year and category label (small caps, tertiary colour) slide in from the left 8px while fading in over the same duration. This is the studio's signature gesture — restrained, typographic, never a hover-zoom image effect. No image ever scales or brightens on hover; only type moves.

LAYOUT — SECTION BY SECTION
Nav: transparent over the hero, fixed, 96px tall, mixing to a solid background-coloured bar with a 1px bottom border once scrolled 80px. Wordmark left in Fraunces; three text links right (Work, Studio, Contact) in small-caps label style.
Editorial hero: full viewport height minus nav. Left-aligned, not centred — a hallmark of the editorial register. Small caps eyebrow label ("Design & brand studio" style) above an oversized Fraunces headline that wraps across two or three lines, max-width roughly 900px. One sentence of body copy in secondary colour beneath, max-width 480px. No CTA button in the traditional sense — a single text link with the clay underline treatment ("View selected work") doubling as the scroll cue. Bottom-right corner of the hero: a small rotating label or the studio's current year/status, tiny, tertiary.
Selected-works grid: an asymmetric grid, not a uniform 3-column — mix a two-thirds/one-third alternating pattern (large project image left, smaller stacked pair right, alternating per row) using CSS grid with explicit column spans, collapsing to a single column, images full-width, on mobile. Each entry uses the signature underline hover from above. Images are large, uncropped-feeling (object-fit: cover but with generous aspect ratios like 4:3 or 16:10, never square-cropped tightly). Six to eight entries.
Services ledger: not a card grid — a numbered list styled like a ledger or index, each service on its own row separated by a 1px hairline border, service name in Fraunces at 28px, a short description in secondary to its right (two-column row layout, stacking on mobile), and a small numeral (01, 02, 03...) in tertiary to the far left. This section deliberately looks like a printed table of contents.
Manifesto block: full-bleed, background surface colour, large centred pull-quote in Fraunces at 40-48px, one to two sentences, with a small attribution line beneath in small-caps. This is the emotional centre of the page — give it the most vertical breathing room of any section (240px padding desktop).
Contact close: left-aligned like the hero, large Fraunces heading ("Let's talk" register), an email link rendered large and underlined in the clay accent, secondary contact details (location, socials) as a small stacked list to the right or below, and a footer bar beneath with wordmark, copyright, and a "back to top" text link.

MOTION DOCTRINE
Framer Motion drives all animation. Hero: headline lines fade and rise 20px individually, staggered 100ms per line, over 700ms cubic-bezier(0.16, 1, 0.3, 1), starting 200ms after mount. Works-grid entries: fade and rise 24px on scroll into view (viewport margin -10%, triggers once), staggered by row. Section headings across the page: same fade-rise-once-on-scroll treatment, 500ms duration. The signature underline hover/reveal as described above is the only motion that repeats on interaction. Page transitions (if a case-study route is later added): a shared-layout crossfade using Framer Motion's layout animations, image morphing from grid position to detail-page hero position over 500ms. prefers-reduced-motion: disable all scroll-triggered fade/rise entirely (render in final state), keep the underline draw but drop it to a flat, non-animated full-width underline that simply appears.

PERFORMANCE BUDGET
First Load JS: under 140KB gzipped for the homepage route (Framer Motion's cost is budgeted in here deliberately; use its LazyMotion + domAnimation feature set rather than the full bundle to keep this honest). LCP target: under 2.2s on throttled 4G — the hero has no image, only type, so this should be very achievable; the works-grid images below the fold must use next/image with lazy loading and explicit width/height, and only the first works-grid image gets priority if it's near the fold. CLS: 0 — reserve aspect-ratio boxes for every work-grid image before load. Fonts: Fraunces and Inter both loaded via next/font with font-display: swap, and Fraunces variable-weight axis subset to only the weights actually used (400, 500, 600) to control file size.

BUILD ORDER
Build one block at a time and stop after each for review.
1. Scaffold the Next.js project, Tailwind tokens, and font loading for Fraunces and Inter; set up the base layout shell and colour/spacing variables.
2. Build the nav, including the scroll-triggered background transition.
3. Build the editorial hero section, static first, then add the staggered headline entrance animation.
4. Build the selected-works asymmetric grid with placeholder images and correct responsive column-span behaviour.
5. Implement the signature clay-underline hover/reveal on the works-grid entries, including the touch-device scroll-triggered variant.
6. Build the services ledger section.
7. Build the manifesto pull-quote block.
8. Build the contact close section and footer.
9. Wire up scroll-triggered entrance animations for every remaining section using Framer Motion with LazyMotion, then do a final performance and reduced-motion pass.`,
    iterationNotes: `Early drafts kept sneaking in rounded corners and drop shadows out of habit — had to explicitly state radius 0 and near-absent box-shadow as non-negotiable, since that flatness is core to the editorial thesis. Also watch the works-grid: the model tends to default to a uniform 3-column grid unless the asymmetric two-thirds/one-third pattern is spelled out with explicit column spans. If Framer Motion's full bundle gets imported instead of LazyMotion + domAnimation, the JS budget will blow out — flag it if you see a plain "import { motion } from 'framer-motion'" without the lazy feature setup.`,
    assetBundlePath: "seed/obsidian-studio.zip",
  },
  starfield: {
    itemId: "00000000-0000-4000-8000-000000000004",
    promptText: `ROLE & STACK
You are building Starfield, a WebGL particle hero scene. Stack: Three.js, React Three Fiber (R3F), GLSL (custom shader material), TypeScript. This is a hero-section-scoped 3D scene, not a full 3D app — it mounts inside a fixed-height hero container alongside normal DOM headline/CTA content layered on top via CSS, and it must degrade gracefully to a static poster image on low-end devices and under reduced motion.

DESIGN THESIS
The single idea: expensive-feeling motion at a cheap-feeling cost. Ten thousand points drifting on a curl-noise field should read as ambient, oceanic, almost meditative — never as a demo-reel showpiece. The scene must never distract from the headline text sitting in front of it; it is atmosphere, not content. Everything about the implementation should be justifiable to a performance-conscious engineer: instancing, GPU-side animation, and a hard fallback.

TOKENS
Colour: canvas background transparent, composited over a page background of #0A0E17 (deep space navy-black). Particle base colour: a cool cyan-white, #CFE8FF, with a secondary accent tint of #4F7CFF mixed in via vertex colour variance (roughly 20% of particles tinted toward the accent, the rest near-white) to give depth without introducing a second competing hue. A single warm accent light source at #FFB27A used sparingly for the DOF-adjacent glow near the pointer, at low intensity — this is the one "breath of warmth" in an otherwise cold palette. UI overlay text: headline #F2F6FF, subhead #93A0B8. Typography for the overlay: "Geist", sans-serif, weight 600 for headline at 52px/1.1 desktop (32px/1.15 mobile), body 17px/1.6 weight 400. Spacing: overlay content constrained to a 640px max-width column, 24px internal gaps. Radius/blur are DOM-overlay concerns only (the CTA button: 8px radius; no blur anywhere in the scene itself — DOF is simulated via point-size and opacity falloff, not a post-process blur pass, to protect the performance budget).

SIGNATURE ELEMENT
The signature moment is pointer parallax with a depth-of-field falloff: as the pointer moves across the hero, the entire particle field shifts opposite the pointer direction by up to 24px of apparent screen-space displacement (implemented as a subtle camera offset, not per-particle displacement, damped with an exponential smoothing factor of 0.06 per frame so it never feels twitchy or 1:1). Particles are distributed across three depth bands; particles in the nearest band have a larger point size (base 3.2px at devicePixelRatio 1, scaled by DPR) and slightly reduced opacity to fake a shallow-depth-of-field blur-via-size-and-alpha technique (no actual blur shader), while the farthest band is smaller and dimmer, creating perceived depth purely through scale, alpha and parallax response rate (near band responds to pointer at 1.0x the offset, mid band 0.5x, far band 0.2x).

LAYOUT — SECTION BY SECTION
Hero container: full viewport height (or a fixed 100vh minus nav height), position relative, containing two stacked layers: the R3F Canvas as an absolutely-positioned full-bleed background layer (z-index 0), and a DOM overlay layer (z-index 10) with pointer-events none except on interactive children.
Canvas/scene setup: a single R3F Canvas, orthographic-feeling perspective camera positioned to keep the particle field readable across aspect ratios (fov roughly 50, camera z around 6), no scene background colour set (transparent, letting the page's CSS background show through so the DOM and WebGL layers colour-match exactly).
Particle system: a single THREE.Points object (or InstancedMesh if per-particle rotation is later desired, but Points is preferred for the budget) built from a BufferGeometry with 10,000 positions distributed within a bounded box roughly matching the hero's aspect ratio, each with a randomised depth-band attribute, a size attribute, and a colour attribute (for the cyan/accent vertex-colour mix). A custom ShaderMaterial: vertex shader displaces each point along a 3D curl-noise field sampled from its own position plus a slow-scrolling time uniform, producing organic, non-repeating drift without any per-frame CPU-side position updates (all animation is GPU-side via the time uniform); fragment shader renders soft circular points (radial alpha falloff from centre) tinted by the per-vertex colour attribute.
Overlay content: headline, one sentence of subhead, and a single CTA button, left-aligned or centred depending on brand needs but defaulting to left-aligned within the 640px column, vertically centred in the hero. A small scroll-cue (a thin animated chevron or line) anchored to the bottom-centre of the hero.
Poster/fallback layer: a statically-rendered PNG or a single frozen frame CSS gradient approximation, absolutely positioned behind the DOM overlay and shown instead of the Canvas when WebGL is unavailable, when prefers-reduced-motion is set, or on devices below a capability threshold (see MOTION DOCTRINE and PERFORMANCE BUDGET).

MOTION DOCTRINE
Continuous motion: the curl-noise drift runs constantly via the shader's time uniform, advancing at a rate tuned so the field completes a perceptible but slow directional drift over roughly 40-60 seconds — never frantic, never obviously looping. Pointer parallax as described in SIGNATURE ELEMENT, damped and band-differentiated. Overlay content entrance: headline and subhead fade and rise 16px over 600ms cubic-bezier(0.16, 1, 0.3, 1), staggered 120ms, starting after the Canvas reports its first successful frame (avoid a flash of overlay-before-scene). prefers-reduced-motion: the Canvas is not mounted at all — render the static poster fallback layer instead, with only a simple CSS opacity fade for the overlay entrance and zero particle motion, zero pointer parallax. This is a hard swap, not a "reduce the animation" — reduced motion means no WebGL context is created at all, which also protects battery and thermals on the devices most likely to have that preference set.

PERFORMANCE BUDGET
Particle/draw-call budget: exactly one draw call for the entire particle system (a single Points draw call for all 10,000 particles — never split into multiple meshes). Target frame budget: sustain 60fps on a mid-range 2022-era laptop GPU and no worse than 30fps with visibly reduced particle count (fall back to 4,000 particles) on integrated/mobile GPUs, detected via a lightweight capability check (renderer string sniff or a first-frame timing probe) rather than assuming desktop. devicePixelRatio capped at 2 to avoid retina overdraw. No post-processing pipeline (no bloom pass, no DOF pass) — all depth/glow effects are baked into point size, alpha and vertex colour as described above, specifically to keep this a single-pass render. First Load JS: Three.js and R3F are heavy — budget 180KB gzipped for this scene's chunk, code-split via dynamic import so it never blocks the initial DOM/overlay paint, and lazy-load it only after the hero container is in viewport (which on a hero is immediate, but the import itself should still be dynamic so the poster/DOM content is interactive first). LCP target: under 2.0s, measured against the overlay headline text (which must be real DOM text, never rendered inside the canvas) since the Canvas itself is explicitly excluded from the LCP-critical path via the poster fallback showing first.

BUILD ORDER
Build one block at a time and stop after each for review.
1. Scaffold the hero container with the DOM overlay content (headline, subhead, CTA) and the static poster fallback layer, no WebGL yet — this must look complete and correct on its own.
2. Add the R3F Canvas, camera setup, and a basic Points system with 10,000 particles at random positions using a plain (non-curl-noise) material, just to confirm the pipeline and draw-call count.
3. Implement the curl-noise vertex shader displacement driven by the time uniform, tuned to the 40-60 second drift cadence.
4. Implement the fragment shader for soft circular points with the cyan/accent vertex-colour mix and the three-depth-band size/alpha falloff.
5. Implement pointer parallax with the camera-offset technique, exponential damping, and per-band response multipliers.
6. Wire up the capability check and reduced-motion hard-swap to the poster fallback, plus the dynamic import code-splitting for the Three.js/R3F chunk.
7. Final pass: overlay entrance animation gated on first-frame render, DPR capping, and a manual frame-budget check across at least one throttled/low-end profile.`,
    iterationNotes: `The biggest risk is the model reaching for a post-processing bloom/DOF pass out of habit, which quietly blows the single-draw-call, single-pass budget — the brief explicitly bans post-processing and requires the depth illusion to come from point size, alpha and vertex colour only, so push back hard if a bloom pass appears. Also verify reduced-motion is implemented as not mounting the Canvas at all rather than just slowing the animation down; that distinction matters for battery and for users who've explicitly opted out of WebGL-heavy pages. Confirm the fallback capability check actually runs before the heavy chunk is fetched, not after.`,
    assetBundlePath: "seed/starfield.zip",
  },
  vellichor: {
    itemId: "00000000-0000-4000-8000-000000000005",
    promptText: `ROLE & STACK
You are building Vellichor, a pack of six animated background layers, each a standalone, drop-in component. Stack: CSS (custom properties, animations, and where useful, backdrop/mix-blend techniques), Canvas 2D (for the layers that need per-frame procedural rendering), TypeScript. Deliver each of the six as its own isolated component with a consistent prop interface (at minimum: an intensity/opacity prop and a static/reduced-motion prop) so any one of them can be dropped behind foreground content on any page.

DESIGN THESIS
The single idea: living backgrounds that never fight the foreground. Every one of these six layers must stay firmly in the background register — soft contrast, restrained colour saturation, and a hard ceiling on how much visual energy they're allowed to spend, because their entire job is to sit quietly behind headline text and UI without ever pulling focus. Each layer has a distinct personality (aurora, mesh, beam, starfield, fog, caustic) but all six share the same discipline: legible foreground first, always.

TOKENS
This pack ships with a shared dark-canvas base since all six layers are designed to sit behind light foreground text: base canvas #0A0A0F. Each layer has its own restrained accent palette, all kept in the same low-saturation, mid-luminosity family so any layer can be swapped in without clashing with a typical dark UI: Aurora drift uses a green-to-violet ribbon pair, #2FE0A8 and #7C5CFF, both capped at 35% layer opacity. Mesh gradient uses a three-point warm/cool blend, #FF6F61, #4F7CFF, #FFD166, each blob capped at 25% opacity with heavy blur. Grain-lit beam uses a single warm beam colour #FFB27A over a fine monochrome grain in #FFFFFF at 4% opacity. Slow starfield uses two star tones, #E7ECF2 and #93A0B8, both small and low-opacity (dots, not glows). Noise fog uses a cool desaturated blue-grey, #4A5568, as a slow-moving Perlin/simplex fog at low contrast. Caustic shimmer uses a pool-light cyan, #6FE3D0, in thin moving refraction lines at low opacity. Typography and spacing tokens are not applicable to this pack (these are pure background layers with no text of their own), but each component's root element must be a strict 100% width/height absolutely-positioned fill of its parent with no intrinsic size of its own. Radius: 0 (full-bleed layers never have rounded corners). Blur: the mesh-gradient layer uses a 60-100px blur on its colour blobs; the noise-fog layer uses a subtle 2-4px blur on its canvas output to avoid banding; the other four layers use no blur.

SIGNATURE ELEMENT
The signature moment across the whole pack is the shared "paint discipline": every layer, regardless of technique, must be implementable to sit comfortably under a 3ms-per-frame paint budget (see PERFORMANCE BUDGET), and every layer must expose the same two props — intensity (0 to 1, scaling opacity/contrast, default 0.6) and static (boolean, renders a single frozen representative frame with zero animation loop when true). The most technically distinct single layer is the caustic shimmer: rendered on Canvas 2D, it draws eight to twelve thin refracted light lines using quadratic bezier curves whose control points drift via layered sine functions (two frequencies per line, phase-offset per line so they never move in visible unison), redrawn at a throttled 24fps (not 60fps — deliberately, since caustic light doesn't need full frame-rate to read as fluid, and this halves its paint cost) with the canvas cleared via a very slight alpha-trail (clearRect replaced with a fillRect at 8% black opacity) to leave a soft motion trail rather than a hard redraw.

LAYOUT — SECTION BY SECTION
Since this is a pack of six independent layers rather than one page, "layout" here means the internal structure of each:
Aurora drift (CSS): two or three large soft-edged radial-gradient or conic-gradient ribbons, positioned absolutely, animated via CSS custom-property-driven translate and hue-rotate keyframes over long durations (45-70s), layered with mix-blend-mode: screen over the dark base so overlaps brighten naturally rather than muddying.
Mesh gradient (CSS): three to four blurred radial-gradient blobs, each independently animated drifting in a slow Lissajous-like path via CSS keyframes (different duration per blob, 30-50s, so they never fall into a repeating unison pattern), composited with mix-blend-mode: soft-light.
Grain-lit beam (CSS + Canvas): a single soft diagonal light beam built as a CSS linear-gradient wedge with a slow positional drift (20-30s ease-in-out loop), overlaid with a fine procedural film-grain generated once per frame on a small Canvas tile (rendered at low resolution, e.g. 128x128, then upscaled/tiled via CSS background-repeat for cheapness) refreshed at a throttled 12fps since grain doesn't need higher.
Slow starfield (Canvas): 150-250 small dots at randomised positions and sizes (1-2px), each with an independent slow twinkle (opacity oscillation, 3-6s per star, phase-randomised) and a very slow collective drift (the whole field pans a few pixels over minutes, not visible frame-to-frame).
Noise fog (Canvas): a low-resolution simplex/Perlin noise field rendered to an offscreen canvas at reduced resolution (e.g. quarter of display size) then scaled up via CSS for a soft, blurred look, animated by advancing the noise's z-coordinate slowly over time (60-90s to feel like a full cycle), redrawn at a throttled 20fps.
Caustic shimmer (Canvas): as described in SIGNATURE ELEMENT above.
Each component ships with a documented default export and TypeScript prop types; all six should be importable from a single index file for convenience.

MOTION DOCTRINE
Every layer's animation loop must be one of: a pure CSS keyframe animation (aurora, mesh, beam's positional drift), or a throttled requestAnimationFrame loop with an explicit fps cap using a timestamp-delta guard rather than relying on the browser's native 60fps tick (starfield's twinkle can run at full rate since it's cheap; fog at 20fps; beam's grain at 12fps; caustic at 24fps, as specified above). Nothing in this pack ever uses a hard cut, flash, or sudden appearance — all six loop seamlessly with no visible restart point, achieved either through modulo-wrapped noise coordinates or through long enough cycle durations that a restart is never observed in a normal page-visit duration. prefers-reduced-motion, or the static prop being set explicitly: every layer renders exactly one representative static frame (for CSS layers, pause the animation via animation-play-state at a fixed frame; for canvas layers, render a single frame and never start the rAF loop) — never hide the layer entirely, since a background layer disappearing on reduced-motion would itself be a visible layout change.

PERFORMANCE BUDGET
Paint budget: under 3ms of main-thread + composite time per frame per active layer, verified via the browser performance panel's paint/composite timings — this is why several layers are throttled below 60fps rather than left to run at native rate, and why canvas-based layers render at reduced internal resolution and scale up via CSS rather than rendering at full device pixel ratio. Only one background layer is expected to be mounted at a time in normal usage (they are alternatives, not meant to be stacked), but each must still be cheap enough that stacking two for a transition crossfade doesn't visibly drop frames. First Load JS: under 6KB gzipped per layer (canvas layers will run higher than CSS-only layers but none should approach 10KB), zero external dependencies (hand-roll the simplex/Perlin noise function rather than pulling in a noise library, to keep the bundle honest). No images. Memory: canvas layers must not accumulate off-screen buffers frame over frame — reuse a single offscreen canvas per instance, cap it at the resolutions specified per layer, and ensure animation loops are cancelled on unmount.

BUILD ORDER
Build one block at a time and stop after each for review, one layer per step.
1. Build the shared component contract first: a base wrapper handling the intensity and static props, the absolute-fill positioning, and the prefers-reduced-motion detection hook, reused by all six layers.
2. Build Aurora drift (pure CSS).
3. Build Mesh gradient (pure CSS).
4. Build Grain-lit beam (CSS beam + throttled canvas grain tile).
5. Build Slow starfield (canvas, full-rate twinkle).
6. Build Noise fog (canvas, throttled 20fps, hand-rolled noise).
7. Build Caustic shimmer (canvas, throttled 24fps, alpha-trail technique).
8. Assemble the index export, verify every layer honours static/reduced-motion correctly, and do a final paint-budget pass across all six.`,
    iterationNotes: `Keeping the paint-budget discipline consistent across all six was the main tuning pass — earlier versions ran every canvas layer at a full 60fps rAF loop, which is unnecessary for slow ambient motion and blew the 3ms target; explicit per-layer fps caps fixed it. Watch for the model reaching for a noise npm package (simplex-noise, etc) instead of a small hand-rolled implementation, which is fine functionally but breaks the stated zero-dependency, sub-6KB-per-layer budget. Also confirm reduced-motion renders a frozen frame rather than unmounting the layer, since a background vanishing is itself a jarring, motion-adjacent change.`,
    assetBundlePath: "seed/vellichor.zip",
  },
  cascade: {
    itemId: "00000000-0000-4000-8000-000000000006",
    promptText: `ROLE & STACK
You are building Cascade, a bento-style feature-grid section component. Stack: React (functional components, hooks), Tailwind CSS, TypeScript. Like Halcyon, this is a drop-in section, not a page — a self-contained component meant to be imported into an existing landing page, exported with a typed prop interface for its six feature cells so the content is fully overridable while the grid geometry and motion stay fixed.

DESIGN THESIS
The single idea: explain a product at a glance through weight, not walls of text. A bento grid works because unequal cell sizes do the hierarchy work that headings usually do — the biggest feature gets the biggest cell, without needing a "flagship" label. Cascade commits to six cells of genuinely different sizes and asymmetric placement (never a uniform 2x3 or 3x2 grid of equal boxes, which would defeat the entire premise), and to a graceful, deliberate reflow down to a single mobile column that still reads as considered rather than collapsed.

TOKENS
Colour: canvas transparent by default (sits on the host page's background), cell surface #14161B, cell surface (largest/hero cell) #171B24 with a subtle 1px inner highlight border at 8% white opacity, standard border #23262E. Accent: a warm amber-gold, #E8A33D, used sparingly for icon strokes, one hover-glow, and small numeric/stat highlights inside cells — never as a cell background fill. Text: primary #EDEEF2, secondary #9498A3, tertiary #5B5F6B. Typography: headings "Geist", sans-serif, weight 600; body "Inter", sans-serif, weight 400. Scale: cell title 20px/600 (24px/600 in the hero cell), cell body 15px/1.55, small stat/label text 13px/500 uppercase tracked. Spacing: 4px base scale. Grid gap 16px both axes. Cell internal padding 28px (32px in the hero cell). Radius: 20px on all cells, consistent regardless of size, which is part of what makes the asymmetric sizes read as one cohesive family rather than mismatched boxes. Border width 1px at #23262E, 1px inner highlight (white 8%) additionally on the hero cell only.

SIGNATURE ELEMENT
Each cell has a soft directional glow that follows the pointer while hovered — a radial gradient centred on the cursor's position within that specific cell (tracked via onMouseMove with clientX/clientY converted to cell-local percentage coordinates), amber #E8A33D at 12% opacity, 280px radius, rendered as a CSS background-image radial-gradient whose position updates via a CSS custom property set from JS (no React re-render per mousemove — write directly to style custom properties for performance). The glow fades in over 200ms on mouse-enter and out over 300ms on mouse-leave, both ease-out. Only one cell's glow is visible at a time in practice since it is pointer-bound, giving the whole grid a "the grid is alive and paying attention to you" feel without any cell ever fully stealing focus from its neighbours.

LAYOUT — SECTION BY SECTION
Section wrapper: max-width 1120px centred, vertical padding 96px (56px mobile), optional section eyebrow/heading/subhead block above the grid (centred or left-aligned, 64px bottom margin) — keep this header block simple, the grid itself is the content.
Grid geometry, desktop (4-column CSS grid, explicit row/column spans): Cell 1 (hero feature) spans 2 columns by 2 rows, positioned top-left — this is the largest cell, gets the 24px title size, a short paragraph, and room for a small illustrative element (an inline SVG diagram or a simple animated stat, per the content prop). Cell 2 spans 2 columns by 1 row, positioned top-right of the hero. Cell 3 spans 1 column by 1 row, positioned to the right of cell 2's row-end. Cell 4 spans 1 column by 2 rows, positioned far right spanning both of the hero's rows. Cell 5 spans 2 columns by 1 row, bottom-left area. Cell 6 spans 2 columns by 1 row, completing the bottom row. (Any equivalent asymmetric arrangement achieving a visually unequal, non-repeating mosaic across a 4-column x 3-row implicit grid is acceptable, but the hero cell must always be the largest single cell and no two cells may share identical dimensions.)
Tablet reflow (768-1024px): collapse to a 2-column grid; the hero cell drops to spanning 2 columns by 1 row (loses its extra height but keeps its width dominance) and the remaining five cells flow in reading order, each 1 column, with cell 4 (the tall one) allowed to span 1 column by 2 rows if it fits naturally, otherwise flattening to 1x1.
Mobile reflow (under 768px): single column, every cell full-width, stacked in a deliberate reading order (hero cell first, then the remaining five in an order chosen for narrative sense, not just source order — expose this as an explicit order prop per cell rather than relying on DOM order, since the desktop visual hierarchy and the mobile reading hierarchy are not always the same sequence). Each cell's internal content simplifies slightly on mobile: any decorative illustration/diagram in the hero cell shrinks or hides below a breakpoint if it can't maintain legibility at full-width-but-shorter proportions.
Cell internal anatomy (consistent across all six): a small icon or numeral top-left (24px, amber stroke, inline SVG, no icon library), title below it, body copy below that, and optionally a small stat/highlight line at the bottom in amber for cells that have a quantifiable claim (e.g. a percentage or a count) — not every cell needs this, only where the content genuinely has a number worth surfacing.

MOTION DOCTRINE
Grid entrance: on mount or on scroll into view (viewport margin -10%, once only), all six cells fade and rise 16px, staggered by grid position (reading order top-left to bottom-right) at 60ms per cell, over 450ms cubic-bezier(0.16, 1, 0.3, 1). The pointer-glow signature element is the only recurring, interaction-driven motion, as described above. Cell hover additionally lifts the cell 2px via translateY and deepens its border to full-opacity white 8% over 200ms, independent of the glow. No cell auto-cycles or auto-highlights without user interaction — this grid is calm until touched. prefers-reduced-motion: disable the mount stagger (render in final position immediately) and disable the pointer-tracking glow's fade transition (it may still appear/disappear but as an instant opacity swap rather than an eased fade); the 2px hover lift is also removed, replaced with a flat border-colour-only hover state.

PERFORMANCE BUDGET
Component JS: under 10KB gzipped excluding React, zero external dependencies, all icons inline SVG. CLS: 0 — grid-template-areas or explicit row/column spans must be defined with fixed track sizing (using minmax with a defined minimum row height) so the grid never reflows as content or images load; any hero-cell illustration reserves its aspect-ratio box up front. The pointer-glow implementation must write to a CSS custom property directly via ref.style.setProperty rather than triggering a React state update per mousemove, specifically to avoid re-render-driven jank — this is a hard requirement, not a nice-to-have, given how frequently mousemove fires. First Load JS contribution: this section should add well under 15KB to whatever page imports it, including its Tailwind-generated utility classes.

BUILD ORDER
Build one block at a time and stop after each for review.
1. Build the static desktop grid geometry with placeholder content in all six cells (no motion, no responsive breakpoints yet) — get the asymmetric column/row spans exactly right first, this is the hardest part to retrofit later.
2. Build the tablet and mobile reflow breakpoints, including the per-cell order prop for deliberate mobile reading order.
3. Build the cell internal anatomy (icon, title, body, optional stat line) as a reusable sub-component consuming a typed content prop.
4. Implement the pointer-tracking glow using the CSS-custom-property-via-ref technique, confirming no per-mousemove React re-render occurs.
5. Add the mount/scroll-triggered entrance stagger animation across the six cells in reading order.
6. Add the hover lift and border-deepen interaction, layered alongside the glow.
7. Final pass: reduced-motion behaviour, prop typing for full content overridability, and a performance check confirming the mousemove handler isn't triggering re-renders (verify via React DevTools profiler or equivalent reasoning).`,
    iterationNotes: `The pointer-glow was originally implemented with React state driving the gradient position, which re-renders on every mousemove and is the single biggest risk to this component's performance budget — it must write directly to a CSS custom property via a ref instead. Also watch the grid geometry: models default to a tidy, equal-sized grid unless explicitly told that no two cells may share identical dimensions, which is the whole point of the bento premise. Double-check the mobile reading order is exposed as an explicit per-cell prop rather than just following desktop DOM order, since the two hierarchies genuinely diverge for this kind of content.`,
    assetBundlePath: "seed/cascade.zip",
  },
};
